@charset "UTF-8";
/* 작성자: 정택준 | 기여자: 김재겸 */
/* Reset default styles */
* {
    margin: 0;
    padding: 0;
    box-sizing: border-box;
}

body {
    font-family: 'Arial', sans-serif;
    line-height: 1.6;
    background-color: #f0f8ff;
    color: #333;
    min-height: 100vh;
    display: flex;
    flex-direction: column;
}


/* 메인 */
main {
    /* 기존 margin 제거 및 중앙 배치를 위한 설정 */
    position: relative; /* 또는 fixed로 고정 가능 */
    top: 80px; /* 헤더 높이만큼 오프셋 */
    left: 260px; /* aside 너비만큼 오프셋 */
    right: 0;
    padding: 2rem;
    max-width: 1280px;
    width: calc(100% - 260px); /* aside 너비를 고려한 너비 */
    min-height: calc(100vh - 80px); /* 헤더 높이 고려 */
    display: flex;
    flex-direction: column;
    justify-content: flex-start; /* 수직 중앙 정렬 */
    align-items: center; /* 수평 중앙 정렬 */
    flex: 1;
    margin: 0 auto; /* 수평 중앙 배치 */
}







